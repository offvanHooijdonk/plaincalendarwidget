<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:windowShowWallpaper">true</item>
    </style>

    <style name="BlockWithBackground">
        <item name="android:paddingStart">@dimen/activity_h_padding</item>
        <item name="android:paddingEnd">@dimen/activity_h_padding</item>
        <item name="android:paddingTop">@dimen/activity_v_padding</item>
        <item name="android:paddingBottom">@dimen/activity_v_padding</item>
        <item name="android:background">@color/settings_activity_blocks_back</item>
    </style>

    <style name="SettingsItem">
        <item name="android:textColor">@color/md_black_1000</item>
        <item name="android:textSize">14sp</item>
        <item name="android:paddingStart">4dp</item>
        <item name="android:paddingEnd">4dp</item>
        <item name="android:gravity">center_vertical|center_horizontal</item>
        <item name="android:textAlignment">center</item>
    </style>

    <style name="SettingsItem.Selected" parent="SettingsItem">
        <item name="android:background">@color/md_blue_grey_200</item>
    </style>

</resources>
